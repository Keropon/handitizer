{
  "name": "handitizer",
  "version": "1.0.0",
  "description": "Turn your hands into live geometry over your webcam (MediaPipe + Three.js).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "copy-deps": "node copy-deps.mjs",
    "predev": "npm run copy-deps",
    "dev": "vite",
    "prebuild": "npm run copy-deps",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35",
    "three": "0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
